run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - gofmt
    - gosimple

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
